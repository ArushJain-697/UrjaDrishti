# ============================================================
# PERSON 2 — Fill this file
# ============================================================
# import joblib
# import pandas as pd
# import numpy as np
#
# mapie_model = joblib.load('models/mapie_cqr.pkl')
# stage2_model = joblib.load('models/lgbm_residual.pkl')
#
# def get_forecast(plant_id: str, hours_of_actuals: int = 0) -> dict:
#     ...
#     return {"plant_id": plant_id, "hours": [...], "p50": [...], "p10": [...], "p90": [...]}
#
# def get_intraday_forecast(plant_id: str, actuals: list) -> dict:
#     ...
#     return {"plant_id": plant_id, "hours": [...], "p50": [...], "p10": [...], "p90": [...]}